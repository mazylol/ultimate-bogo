#include <iostream>
#include <vector>

#include "Bogo.h"

int main() {
    /*std::vector<int> vec = {3, 5, 1, 9, 6, 8};

    Bogo::sort(&vec);

    for (auto &item : vec) {
        std::cout << item << " ";
    }
    std::cout << "\n";*/

    std::cout << "Hello, World!" << std::endl;

    return 0;
}